services:
  backend:
    build:
      context: ./backend
    container_name: restaurant-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: restaurant-frontend
    ports:
      - "8081:80"
    depends_on:
      - backend
    restart: unless-stopped